<!DOCTYPE html>
<html>
<head>
  <title>LocaPeople</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<canvas id="landing-canvas"></canvas> 
<script>

	var canvas = document.getElementById('landing-canvas');
	var context = canvas.getContext('2d');
	var imageObj = new Image();
	imageObj.onload = function() {
	  context.drawImage(imageObj, 0,0,imageObj.width,imageObj.height,0,0,canvas.width,canvas.height);
	  };
	imageObj.src = 'assets/image01.jpg';

     //  var canvas = document.getElementById('landing-canvas');
     //  var context = canvas.getContext('2d');
     //  var imageObj = new Image();
     //  imageObj.src = 'assets/image01.jpg';

     //  imageObj.onload = function() {
     //  	var pattern = context.createPattern(this, "repeat");
    	// context.fillStyle = pattern;
    	// context.fill();
     //    // context.drawImage(imageObj, 100, 100);
     //  };


    </script>

<%= yield %>

</body>
</html>


